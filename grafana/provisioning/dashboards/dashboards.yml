apiVersion: 1

providers:
  - name: admin-usage-dashboards
    orgId: 1
    folder: Admin
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
